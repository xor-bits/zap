// immutable compile-time static
val := { 4 };

main := (i32) -> (i32) {
    print("Hello, world!" + val);
};


mul_2 := i32 -> i32 {
	// `$` is the nameless fn argument
	$ * 2
};

sum := (a: i32, b: i32) -> i32 {
	a + b
};
